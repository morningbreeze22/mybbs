<h2>我的收藏：</h2><br>
<div id="myfavor">
<% @forums.each do |f| %>
  <tr>
    <td><%= f.name %></td>
    <td><%= link_to '进入', f %></td>
    <td><%= link_to "取消收藏", "/unfavor/"+f.id.to_s, method: :post %></td><br>
  </tr>
<% end %>
</div>
<%= link_to "返回", root_path %>